import express, {Router} from 'express';
const router:Router = express.Router();

/*

import {{prefix}}Controller from '../controllers/{{prefix}}Controller';

router.get('/test', {{prefix}}Controller.sample)

*/

export default router;