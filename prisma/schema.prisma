generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = env("DB_DIALECT")
  url      = env("DATABASE_URL")
}

model Index {
  id    Int     @id @default(autoincrement())
  name String  @unique
  description  String?
}

